'use client';
// KPI Section is now integrated into the Hero component
// This component is kept for backward compatibility but renders nothing standalone
export default function KPISection() {
  return null;
}
